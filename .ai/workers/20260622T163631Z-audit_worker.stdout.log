我现在拥有完整画面。以下是审计报告。

---

## 审计结果：PASS

经过对 31 个变更文件（2 个后端源文件、1 个前端测试、28 个二进制截图/报告）的审查，**未发现阻塞性缺陷**。变更在边界内是正确且安全的。

---

### 1. WMS 主产品链

**人工入库 — SubProject 自动创建 ✅**
`service.ts:442-454` — 当提供`departmentId`和`projectName`但未提供`subProjectId`时，代码会在租户范围内按`departmentId + name`查找现有 SubProject，或创建一个新的。仅在首次入库时执行一次；在往返编辑/删除过程中不会重复创建。项目名称通过 inventory `projectName`字段保留。

**出库 — 班组信息保留 ✅**
`service.ts:1044` — `workTeamName` 现在直接从出库创建时的查找结果写入 `OutboundOrder`。该字段已存在于 schema 中（prisma 第 619 行），因此无需迁移。台账查询使用该字段进行显示和搜索。

**退库 — 已删除不存在的字段 ✅**
`service.ts:1234-1237` 和 `service.ts:1269-1271` — `unitPrice`、`unit` 和 `projectName` 已从 `ReturnItem` 创建数据中移除。与 schema 一致（`return_items` 表仅有 `id`、`returnOrderId`、`outboundItemId`、`materialId`、`quantity`）。**注意**：Excel 退库路径（第 1267 行）同样已被正确清理。

**班组台账 — 扁平结构 + 关键词过滤 ✅**
`service.ts:1515-1553` — 返回一个平坦的 `{id, workTeamName, materialName, unit, quantity, unitPrice, totalAmount, outboundDate, projectName, type, returnedQuantity}` 形状，不再嵌套 Prisma 对象。关键词过滤在三个字段上使用 `toLowerCase()`，不区分大小写。

**关键字：** 路线变更中新增的 `keyword` 查询参数（`routes.ts:1237`）在前端已存在支持（`Ledger.tsx:36` — 传入 `wmsApi.getWorkTeamLedger({ ..., keyword })`）。

---

### 2. 浏览器覆盖真实性

**Playwright 配置：** `channel: 'chrome'` → 使用系统 Google Chrome，**非**无头 Chromium 或纯 API 模式。✅

**测试流程：**
1. 创建供应商 → 填写项目部/物资/数量/项目名称 → 确认入库 → 验证"入库成功" + 表格行可见 → 打开明细
2. 导航至出库 → 选择项目部（通过多选弹窗）→ 选择班组 → 勾选物资 → 填写数量 → 确认出库 → 验证"出库成功" → 下载出库单 PDF（验证文件名匹配 `/^出库单-.*\.pdf$/`）→ 关闭弹窗 → 验证表格显示
3. 导航至退库 → 按名称选择子项目 → 查询领料记录 → 勾选物资 → 确认退库 → 验证"退库成功"
4. 导航至调拨 → 选择调出/调入（通过弹窗）→ 勾选物资 → 确认调拨 → 验证"调拨成功" → 下载调拨单 PDF（验证文件名匹配 `/^调拨单-.*\.pdf$/`）
5. 导航至台账 → 按物资名称搜索 → 验证行可见

**结论：** 这是真实浏览器端到端覆盖，非烟雾测试。截图保存路径：`docs/smoke-evidence/物资主链路CRUD.png`。所有 9/9 测试通过。

---

### 3. 风险

**租户隔离 ✅**
- SubProject 查找/创建使用 `tenantId: data.tenantId`（第 447-450 行）
- 台账查询使用 `outboundOrder: { tenantId }`（第 1505 行）
- 与现有模式一致，无突破

**模块权限边界 ✅**
- WMS 路线保留 `requirePermission(...)` 守卫
- 无新增暴露端点

**库存核算回归 ⚠️ 非阻塞，需注意**
- **`service.ts:1518-1523`** — `listWorkTeamLedger` 现在在内存中获取**所有**匹配的 `OutboundItem` 记录，然后进行分页。这是必要的，因为计算 `netQuantity` 需要聚合返回数量，而关键词过滤无法在 SQL 中高效完成。对于大型租户（数千条出库记录），此调用会随着时间推移而变慢。
- **影响因素**：`pageSize` 限制（默认 20）限制了内存影响——剔除的数据永远不会序列化。这比返回完整 Prisma 对象更高效，因为对象已被扁平化。
- **缓解措施**：这是一个 MVP 基线。在可接受之前无需修复，但应排入性能路线图。

**数据丢失/破坏性行为 ❌ 未发现**
- 无级联删除变更
- 无架构变更（`workTeamName` 已存在）
- ReturnItem 字段移除纠正了先前存在的错误（写入不存在列时，Prisma 会静默忽略），因此无法恢复数据丢失

**公开仓库密钥泄露 ✅ 清晰**
- 仅代码变更涉及路由/服务逻辑和测试文件
- 测试凭证（`admin/Admin@2024`）明显为演示凭据
- 无 `.env` 文件、API 密钥或机密被跟踪
- 二进制截图来自演示租户 — 确认非生产数据

---

### 高风险 — 应该修复

1. **`service.ts:1525-1534` — 出库台账导出仍使用旧形状**
   `getWorkTeamLedgerExportData`（第 1531+ 行）返回嵌套的 Prisma 对象，使用了 `item.outboundOrder.workTeamName` 的 `workTeamName` 导出。API 使用者将收到与 `listWorkTeamLedger` 不同形状的数据。导出格式与台账显示输出之间可能存在不一致。**建议：** 重构导出以使用相同的扁平形状，以保持一致性。

---

### 摘要

| 类别 | 结果 |
|--------|--------|
| 通道 | **PASS** — 无阻塞性问题 |
| 高风险 | 1 — 导出/台账形状不一致，需重构 |
| 中等 | 1 — 台账需要在 DB 级别进行内存分页（已知 tradeoff） |
| 低 | 0 |
| 测试 | 9/9 通过，真正的 Chrome E2E 覆盖五大 WMS 操作 |
| 安全 | 干净的公开仓库，无密钥，租户隔离到位 |
