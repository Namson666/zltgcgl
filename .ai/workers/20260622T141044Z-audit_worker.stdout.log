审查完成。以下是详细报告。

---

## Phase 4 Hardening 切片审查报告

### 阻塞问题：无

所有变更均为**新增**，未修改已有的 GET/POST 路由或逻辑。风险很低，可以继续提交。

---

### 租户隔离检查

| 端点 | 中间件 | 隔离方式 | 结论 |
|------|--------|----------|------|
| `DELETE /.../trusted-locations/:id` | `authenticate` + `requireUser` + `canManageAttendance` | `findFirst({ id, tenantId })` | ✅ |
| `GET /.../trusted-locations` | 同上 | `listTrustedCheckInLocations(tenantId, ...)` | ✅ |
| `POST /.../trusted-locations` | 同上 | `addTrustedCheckInLocation({ tenantId, ... })` | ✅ |

### 非阻塞建议

**建议 1（防御性）— `delete` 未附带 `tenantId`**

```typescript
// service.ts:1202 — 当前做法
await prisma.trustedCheckInLocation.delete({ where: { id } });

// 建议改为 deleteMany 附带 tenantId，一次性原子操作
const result = await prisma.trustedCheckInLocation.deleteMany({ where: { id, tenantId } });
if (result.count === 0) throw { status: 404, ... };
```

当前写法有 `findFirst` 兜底，不存在安全问题，但 `findFirst` + `delete` 存在 TOCTOU 窗口，且未来如果有人重构删掉了 `findFirst`，删除就变成跨租户的。`deleteMany` 是单次原子操作，消除隐患。**非阻塞，建议修复。**

**建议 2（样式）— 统一 `TrustedLocation` 类型**

`Attendance.tsx:56` 内联的 `TrustedLocation` 接口与后端实际返回字段不完全匹配。后端 `listTrustedCheckInLocations` 通过 `Prisma include: { personnel }` 返回完整 personnel 对象（含 `id, name, phone`），但前端接口定义缺少 `personnel?.phone` 之外的其他字段。虽然不影响功能，建议与后端对齐。

**建议 3（非阻塞）— 删除无确认弹窗**

用户点击红色删除按钮直接执行 API 请求，没有二次确认。建议加一个确认步骤（`confirm()` 或 Modal），避免误操作。

---

### 原有功能回归风险

**低风险。** 变更全部是新增：
- Routes：新增 1 条 DELETE（不影响已有的 GET/POST/日历/规则等路由）
- Service：新增 1 个函数（不影响已有的 30+ 函数）
- Frontend Attendance.tsx：新增 `trustedLocations` 状态（不影响日历/打卡记录/异常处理）
- Frontend Personnel.tsx：`facePhotoUrl` 是从 `<CheckCircle>文本` 改为 `<a><img></a><div><CheckCircle>文本</div>`，语义无损

---

### Smoke 覆盖分析

| 路径 | 覆盖情况 |
|------|----------|
| 小程序打卡照片入口可见 | ✅ `getByRole('link', { name: '查看照片' }).first()` |
| 添加信任地后列表可见 | ✅ `getByText('{personName} · 广东省深圳市宝安区')` |
| 删除信任地成功 | ✅ `getByTitle('删除信任地').click()` → `getByText('个人信任打卡地已删除')` |
| 开发者默认小程序配置 | ✅ `fill('wx_smoke_default_...')` + `保存默认小程序配置` |
| 企业小程序配置 | ✅ `fill('wx_smoke_tenant_...')` + `保存企业小程序配置` |

前端 `Attendance.tsx:238` 的 `photoUrl` 条件渲染依赖后端 `listMobileCheckIns` 返回的 `photoUrl` 字段。后端查询（service.ts:1130）使用 `findMany`（返回所有字段），且创建时（1327行）已存入 `photoUrl: data.photoUrl`，数据流完整。✅

**Smoke 测试中有一处可能问题**：line 248 `page.getByTitle('删除信任地').click()` — 按钮 `title` 为中文"删除信任地"，但 button 的 `title` 属性的作用是工具提示而非可访问名称。在高分辨率/小视口下 button 可能被隐藏或不可见，Playwright 会报 `strict mode violation` 或有多个匹配。测试中只有一个删除按钮（`trustedLocations.length > 0` 才渲染），所以理论上只有一个匹配项。风险低。

---

### 是否可继续提交

**可以。** 没有阻塞问题。如果希望更稳健，建议修复建议 1 的 `deleteMany` 模式（1 行改动）。
