你是独立审查 worker。请审查本轮改动是否正确推进“全功能真实浏览器验收交付”目标，重点是财务类别设置和回收站深度覆盖。

仓库：/Users/Namson/.config/superpowers/worktrees/资料通工程管理系统/public-clean-baseline

本轮改动：
- `frontend/src/pages/finance/CategorySettings.tsx`
  - 首次加载主类别时自动加载第一个主类别的子类别，避免右侧空白/过期。
  - 主类别/子类别编辑删除按钮增加带名称的 title/aria-label，便于真实浏览器可访问点击。
- `backend/src/modules/recycle-bin/routes.ts`
  - 回收站列表兼容 `keyword` 查询参数；前端原本传 keyword，后端只读 search，导致搜索可能无效。
- `backend/src/common/middleware/rateLimit.ts`
  - 增加 `DISABLE_RATE_LIMIT=true` 的显式跳过逻辑。
- `scripts/start-browser-smoke-server.sh`
  - 仅 browser smoke 测试服务器设置 `DISABLE_RATE_LIMIT=true`，避免全量真实 Chrome smoke 请求量增加后用 429 掩盖模块未开通 API 403 验证；生产默认不变。
- `frontend/tests/smoke/browser-smoke.spec.ts`
  - 新增真实 Chrome 流程：费用大类创建/编辑/删除、费用子类创建/编辑/删除、合同软删除进入回收站、回收站搜索、恢复、再次删除、永久删除、API 验证不再存在。

已执行证据：
- 聚焦 Chrome：`BROWSER_SMOKE_DB_PATH=/private/tmp/zlt_browser_smoke_category_recycle.db npx playwright test --config playwright.config.ts -g "finance categories and recycle bin"` 1/1 通过。
- 全量真实 Chrome：`bash scripts/browser-smoke.sh` 13/13 通过。
- `bash scripts/verify.sh` 通过。
- `code-review-graph build --skip-flows && code-review-graph detect-changes` 风险 0.50。

请检查：
1. 功能是否真的覆盖财务类别设置和回收站关键 CRUD/恢复/永久删除路径。
2. 回收站 keyword/search 修复是否正确且向后兼容。
3. `DISABLE_RATE_LIMIT` 是否只影响测试环境，生产默认是否仍保留限流。
4. 是否有权限/租户隔离/误删风险或假成功。
5. 若有 blocker 请明确给出；否则 PASS。

输出：
- PASS 或 BLOCKED
- blockers（如无 none）
- yellow risks（如有）
- 简短理由
