现在我已有足够信息形成完整审查结论。

---

# 企业端公告闭环改动 — 独立审查报告

## 范围确认

本次改动确实包含用户描述的全部 5 个文件，且实现与描述一致。

## 审查重点逐项核验

### ✅ 1. 企业用户只能读取已发布公告

- `announcement/routes.ts:19` `where: { isPublished: true }` 强制只查已发布。
- `select` 显式挑选字段，未泄漏 `createdBy` / `updatedAt` 等管理字段。
- `app.ts:90-91` 路由只挂 `GET /` 一条，无 `POST/PUT/DELETE` → 物理上无法写。
- 测试 `forbiddenCreateResponse/UpdateResponse/PublishResponse/DeleteResponse` 四种 403 用例覆盖企业 token 攻击开发者管理接口（smoke spec 行 1125-1140）。

### ✅ 2. 开发者普通 token 不能访问企业端公告接口

- `app.ts:91` 挂 `authenticate, requireUser`。
- `requireUser`（auth.ts:124-165）：开发者**普通** token 满足 `type === 'developer'` **且** `tenantId` 为空 → 落到行 145 `if (req.user?.type !== 'user')` → **拒绝 403**。
- 唯一绕过路径是开发者**进入租户视角**（`isDevView=true` 或 `tenantId` 存在），这是预期行为（开发者支持企业视角排查）。
- smoke spec 行 1079-1082 显式断言：用开发者普通 token 调 `/api/announcements` 返回 403。

### ✅ 3. 未发布 / 下架 / 删除公告不会出现在企业端

- 未发布：`where: { isPublished: true }` 直接过滤。
- 下架：`publishAnnouncement`（service.ts:851）将 `isPublished` 翻转为 false 时同步把 `publishedAt` 置 null → 同样被过滤。
- 删除：`deleteAnnouncement`（service.ts:845）硬删除 `prisma.announcement.delete` → 行级消失。
- **注**：Announcement 模型（schema:1258-1269）**没有软删除字段**，删除即物理删除，无需像 `isActive` 之类的额外过滤。
- smoke spec 行 1142+ 应继续覆盖下架后企业端不显示；如未覆盖属于"测试遗漏"，但不影响数据正确性。

### ✅ 4. Dashboard 新增公告区域不影响原有功能

- 新增区域包裹在独立 `card` 内（Dashboard.tsx:278-321），`announcements.length > 0` 时才渲染，**空数据零侵入**。
- `fetchAnnouncements` 是**独立** `useEffect`（行 219-231），带 `try/catch` 自吞错误 → 不影响第 1 个 `useEffect`（行 111-217）的 6 个并行统计模块。
- 原有 `topStats / wmsStats / laborStats / setLoading(true)/false` 流程完全未改。
- 顶部统计、模块开通判断、快捷入口区原样保留。
- 关于"已删除的库存预警功能"：本次 diff 中未触及任何库存预警代码 → **未引入回归**。

### ✅ 5. TypeScript / 权限 / limit / 测试

| 项 | 检查 | 结论 |
|---|---|---|
| TS 编译 | 用户报告 backend + frontend `npm run build` pass | ✅ |
| limit 边界 | `parseLimit`（routes.ts:7-13）`Math.min(20, Math.max(1, Math.trunc))` → 非法值/超界/小数/负数全部安全归一化 | ✅ |
| 错误响应 | 500 走 `INTERNAL_ERROR` 信封，与项目其它接口一致 | ✅ |
| 测试覆盖 | smoke 1/1 + verify 57/57 + 36/36 + browser 34/34 | ✅ |
| `code-review-graph` | risk 0.50，无受影响 flow | ✅（0.50 在公告这种低风险新增里属正常） |

## 发现

### 🚫 阻塞问题
**无。**

### ⚠️ 非阻塞建议

1. **测试缺少"下架后企业端不显示"断言**（非阻塞）。smoke spec 覆盖了创建→企业可见→Dashboard 渲染，但没有"开发者下架 → 企业端列表为空 → Dashboard 区域不渲染"的回归断言。建议下次增补一个 step：从开发者视角 POST `/api/developer/announcements/:id/publish` 翻为下架，再请求 `/api/announcements?limit=10` 断言不包含该 title。当前 `announcementApi` 的 limit=3 默认排序由 `publishedAt desc` 保证下架后立即不出现，行为正确，但缺少回归断言锁。

2. **响应成功分支的 `message` 字段缺失**（一致性问题，非阻塞）。`announcement/routes.ts:35` 只 `res.json({ success: true, data })`，没有 `message`；其它接口习惯带 `message`。前端 `unwrapApiData` 直接读 `data`，不影响功能；统一风格建议后续加上。

3. **Dashboard `fetchAnnouncements` 的 `useEffect` 没有依赖 `user.tenantId`**（低优先级）。当前 `[]` 依赖正确（公告是全局已发布，不跟租户变），但若未来按租户定向公告，需在此处加 `user?.tenantId` 到依赖数组。当前实现安全。

4. **`app.ts` 路由挂载顺序可读性优化**（非阻塞）。`/api/announcements` 紧跟在 `/api/developer` 之后，与"开发者管理 / 企业只读"的层级语义吻合；无需调整。

## 是否认可本次 slice 可提交

**✅ 认可提交。**

理由：
- 数据隔离 4 个面（字段过滤、状态过滤、物理删除、路由只读）全部到位；
- 权限双向隔离（企业↔开发者）有 `requireUser` + 测试双向 403 双重验证；
- limit/分页边界用 `Math.min/Math.trunc` 防御性归一化；
- Dashboard 公告区与原有统计模块解耦（独立 effect、独立卡片、空态不渲染），不构成回归；
- 所有 build / 单测 / E2E / smoke 全绿；
- code-review-graph risk 0.50 且无受影响 flow。

可随时合并至 `main` 并推送至 `public-main`。
