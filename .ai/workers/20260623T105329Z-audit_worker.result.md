# Claude Worker Result: 20260623T105329Z-audit_worker

- role: `audit_worker`
- status: `PASS`
- exit_code: `0`
- prompt: `.ai/workers/20260623T105329Z-audit_worker.prompt.md`
- stdout: `.ai/workers/20260623T105329Z-audit_worker.stdout.log`
- stderr: `.ai/workers/20260623T105329Z-audit_worker.stderr.log`
- diff: `.ai/workers/20260623T105329Z-audit_worker.diff.patch`

## Worker stdout excerpt

```text
已获取全部关键证据。基于全面代码审查与 chrome smoke 测试覆盖，输出审查结论。

---

# 审计结论：PASS

## BLOCKERS
无

## YELLOW RISKS（注意但不阻塞）

1. **后端 service.ts 文件体量大（1665+ 行）** — 单文件包含物资/库存/送货/入库/出库/退库/调拨/班组/台账所有业务逻辑，调试时定位慢。建议下个迭代按领域拆分 `inventory.service.ts` / `outbound.service.ts` 等。
2. **`deleteInboundOrder` / `updateInboundOrder` 仍用 `prisma.inventory.update` 而非 findFirst + update 模式**（service.ts:819, 821, 854, 911, 927, 958） — 与 PF-304 教训不一致：复合唯一键 `subProjectId_materialId_projectName` 含可空字段，理论上同踩坑风险。但现有 `inventory.findFirst` 在每次 update 前已执行（service.ts:815-817, 838-841, 953-955），实际等价安全。需要后续抽 helper 收敛模式。
3. **`getOutboundExportData` 使用 `mode: 'insensitive'`（service.ts:1189）** — SQLite 在 Prisma 上忽略 mode 参数（行为退化为 case-sensitive），与 `listWorkTeamLedger` 在 service.ts:1640-1644 完全改用客户端 toLowerCase().includes() 形成不一致模式。Chrome smoke 测试用中文物资名不暴露此问题，但英文场景下出库导出与台账列表搜索结果可能不一致。建议把 `getOutboundExportData` 也改成客户端过滤。
4. **`getOutboundExportData` 不分页全量查询（service.ts:1191）** — 与台账一样取全部，未应用分页上限。当单租户出库单很多时内存压力大。
5. **`getMaterialsExportData` 无搜索过滤（service.ts:124-133）** — 当前 frontend 导出按钮也不传 keyword，行为一致但接口不一致。

## 简短证据

### 1. 库存导出复用 active stock 语义 & SQLite-safe contains ✅
- `getInventoryExportData` → `listInventory`（service.ts:269）共用同一过滤函数，AND 数组结构 `{ material: { tenantId } }, { isActive: true }, { quantity: { gt: 0 } }`（service.ts:215-219）。
- 测试断言（service.test.ts:270-280）：`isActive: true`、`quantity: { gt: 0 }`、`material: { name: { contains: '钢筋' } }`，无 `mode` 参数。
- 测试断言（service.test.ts:76-86, 228-247）：`listMaterials` 与 `listInventory` 都用纯 `contains`，无 unsupported mode。
- Routes.ts:486-510 把 `materialName || q.keyword` 传给后端，前端 Materials.tsx:245 透传 `keyword`，复用当前过滤语义。

### 2. 班组台账只使用 active 出库 & 退库 active 数量 ✅
- `listWorkTeamLedger` where（service.ts:1600-1601）：`outboundOrder: { tenantId, isActive: true }`。
- 退库聚合（service.ts:1617-1620）：`returnOrder: { isActive: true }`，净数量 = `item.quantity - returnedQuantity`。
- `getWorkTeamLedgerExportData` 复用 `listWorkTeamLedger`（service.ts:1651），保证导出与列表完全同源。
- 测试断言（service.test.ts:294-368）：outboundItem 查询带 `outboundOrder.isActive: true`，returnItem.aggregate 带 `returnOrder.isActive: true`，净数量 = 3，已退 = 1。

### 3. 前端导出按钮下载 blob、文件名、toast 与测试一致 ✅
- `handleExportInventory`（Materials.tsx:242-260）：`downloadBlob(res.data as Blob, ...)` + `toast.success('库存记录已导出'/'已出库记录已导出')`。
- 文件名分支：`tab === 'out' ? '已出库库存.xlsx' : viewMode === 'detail' ? '在库明细.xlsx' : '库存汇总.xlsx'`。
- `handleExportLedger`（Ledger.tsx:53-64）：`downloadBlob(res.data, '班组台账.xlsx')` + `toast.success('班组台账已导出')`。
- API 客户端均使用 `responseType: 'blob'`（api/index.ts:837, 848, 961, 1027）。
- Chrome smoke（browser-smoke.spec.ts:2222-2225, 2230-2233, 2249-2252）断言文件名 + toast 完全匹配。

### 4. Chrome smoke 覆盖 10/4/1→库存7、净领用3、3 个下载 ✅
- browser-smoke.spec.ts:2157-2214：入库10 → 出库4 → 退库1 → 库存 `quantity === 7`（行 2214）。
- 行 2241：ledger `quantity === 3`（净领用），行 2242 `returnedQuantity === 1`。
- 三个下载断言：库存汇总.xlsx（行 2224）、已出库库存.xlsx（行 2232）、班组台账.xlsx（行 2251），全部走 `page.waitForEvent('download')` + `suggestedFilename()`。

### 5. 库存预警/低库存功能未恢复 ✅
- 全仓 `grep -rn "存货预警|低库存|stockAlert|lowStock|inventoryAlert"` 在 `backend/src/` 与 `frontend/src/` 均无命中。
- Materials.tsx 状态列只有 `badge-green '正常'`（行 384-386），无预警阈值展示逻辑。CLAUDE.md 中近期修复列表也未提及此功能，属于"未引入"。

### 验证产物 ✅
- backend service.test.ts: 11 个 it 全覆盖活性过滤、tenant 隔离、SQLite-safe contains、净数量切片。
- backend verify.sh: 7 files / 50 tests + build pass。
- frontend verify.sh: 2 files / 36 tests + build pass。
- full Chrome smoke 29/29 pass；本次新增 wms-export-ledger 测试 1/1 pass。
- code-review-graph risk: 0.50（中等）。

```

## Worker stderr excerpt

```text

```
