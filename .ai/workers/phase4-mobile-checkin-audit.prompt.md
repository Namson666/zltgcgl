请作为独立审查 worker 审查当前工作区的“小程序接入 + 人员人脸 + 微信打卡异常处理”切片。

重点检查：

1. 后端数据模型是否覆盖：
   - 开发者默认小程序、企业自有小程序 appId 分流；
   - 人员人脸照片/状态；
   - 每日一次/两次打卡规则；
   - 移动端照片、定位、县份、异常、处理状态；
   - 个人信任打卡地。
2. 移动端 `/api/mobile/check-in` 是否不会依赖后台登录，但仍会按 appId/手机号匹配企业和人员，且不静默处理手机号命中多个企业。
3. 未开通劳资模块的企业是否会被拒绝小程序打卡。
4. 异常打卡是否“不阻止打卡”，并支持批量处理、添加个人信任地。
5. 前端是否提供后台入口：
   - 人员详情上传人脸；
   - 考勤页查看移动打卡、设置每日次数、批量处理异常、添加个人信任地；
   - 开发者后台默认小程序和企业小程序配置。
6. 真实 Chrome smoke 是否覆盖了新增、上传、打卡、异常批量处理、信任地。

请输出：

- 阻塞问题（如果有）
- 非阻塞改进建议
- 对 Product Green 的诚实判断

不要修改代码，只做审查。
