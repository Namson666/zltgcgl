你是独立终审 worker。请审查最终本轮改动是否正确推进“全功能真实浏览器验收交付”目标，重点是财务类别设置和回收站。

最终改动：
- `frontend/src/pages/finance/CategorySettings.tsx`
  - 首次加载主类别时自动加载第一个主类别的子类别。
  - 主类别/子类别编辑删除按钮增加带名称的 title/aria-label。
- `backend/src/modules/recycle-bin/routes.ts`
  - 回收站列表兼容 `search` 和 `keyword` 查询参数。
- `backend/src/common/middleware/rateLimit.ts`
  - 增加 `DISABLE_RATE_LIMIT=true` 的显式跳过逻辑，默认不跳过。
- `scripts/start-browser-smoke-server.sh`
  - 仅 browser smoke 后端启动时设置 `DISABLE_RATE_LIMIT=true`。
- `frontend/tests/smoke/browser-smoke.spec.ts`
  - 新增真实 Chrome 用例：费用大类创建/编辑/删除、费用子类创建/编辑/删除、API 读回校验、合同软删除进入回收站、UI keyword 搜索、直接 `/api/recycle-bin?keyword=...` 校验、恢复、再次删除、永久删除、最终 API 验证不再存在。

最终证据：
- 聚焦 Chrome：`BROWSER_SMOKE_DB_PATH=/private/tmp/zlt_browser_smoke_category_recycle_keyword.db npx playwright test --config playwright.config.ts -g "finance categories and recycle bin"` 1/1 通过。
- 全量真实 Chrome：`bash scripts/browser-smoke.sh` 13/13 通过。
- `bash scripts/verify.sh` 通过。
- `code-review-graph build --skip-flows && code-review-graph detect-changes` 风险 0.50；函数级 gap 由真实 Chrome 产品路径覆盖。

请检查：
1. 是否有 blocker：权限、租户隔离、误删、假成功、生产限流被关闭、搜索不兼容等。
2. 本轮是否真实覆盖了财务类别设置和回收站关键路径。
3. 是否应允许提交/推送。

输出：
- PASS 或 BLOCKED
- blockers（如无 none）
- yellow risks（如有）
- 简短理由
