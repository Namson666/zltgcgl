我现在已经掌握了所需的一切信息。以下是审计报告。

---

## Phase 3 合同附件审计报告

### 结论：**PASS** — 无阻塞性问题。发现 2 个小问题和 1 个中等严重程度的错误处理问题。

---

### 1. 租户隔离与文件访问安全性

**通过。** 所有附件服务功能均按 `tenantId` 过滤：

- `listContractAttachments` — `where: { tenantId, entityType: 'contract', entityId: contractId }`（`service.ts:240`）
- `getContractAttachment` — `where: { id: attachmentId, tenantId, entityType: 'contract' }`（`service.ts:246`）
- `deleteContractAttachment` — 通过 `getContractAttachment` 调用，继承了相同的过滤条件（`service.ts:251`）
- `createContractAttachments` — 先在 `assertContractExists` 中验证了合同是否属于该租户（`service.ts:212-213`）
- 文件路径生成使用服务器生成的 UUID（`uuidv4()+ext`），因此不存在路径遍历的风险（`routes.ts:30`）

### 2. 遗留的劳务模块耦合问题

**无。** 前端已正确切换：
- `List.tsx:41` — 导入从 `laborApi` 改为 `contractApi` + `downloadBlob`
- `List.tsx:328` — `laborApi.getAttachments` → `contractApi.getAttachments`
- `List.tsx:354` — `laborApi.uploadAttachment` → `contractApi.uploadAttachment`
- `List.tsx:371` — `laborApi.deleteAttachment` → `contractApi.deleteAttachment`
- `List.tsx:381` — 新增了 `contractApi.downloadAttachment` 调用（之前没有）
- 表单字段从 `entityType/entityId` 改为 `contractId/category`（`List.tsx:351-352`）

### 3. 前端 API 接口是否正确

**是的。** `contractApi` 中定义的所有 4 个方法（`api/index.ts:646-675`）与后端的路由和 HTTP 客户端签名完全匹配：

| 方法 | URL | 匹配后端？ | 签名正确？ |
|--------|-----|--------------|----------------|
| `getAttachments` | `GET /contracts/attachments?contractId=` | ✓ `routes.ts:156` | ✓ |
| `uploadAttachment` | `POST /contracts/attachments/upload` | ✓ `routes.ts:178` | ✓（`http.upload` → `multipart/form-data`） |
| `downloadAttachment` | `GET /contracts/attachments/:id/download` | ✓ `routes.ts:206` | ✓（`responseType: 'blob'`） |
| `deleteAttachment` | `DELETE /contracts/attachments/:id` | ✓ `routes.ts:233` | ✓ |

前端现在使用 `downloadBlob()`（从 `@/api` 的 `api/index.ts:1323` 导出）来处理下载响应——正确。

### 4. 测试覆盖范围

烟雾测试在 `browser-smoke.spec.ts:46-73` 中创建了一个完整的 CRL 流：

1. 创建合同 → 验证可见性
2. 打开详情 → 上传 `contract-attachment.pdf`（471 字节，真实 PDF v1.4）→ 验证按钮可见性
3. 通过 `waitForEvent('download')` 下载 → 断言 `suggestedFilename()`
4. 删除附件 → 验证计数为 0
5. 删除合同 → 验证文本消失

会话的目录结构是正确的——`frontend/tests/fixtures/contract-attachment.pdf` 存在且是一个有效的 PDF。**未检测到伪造完成。**

### 5. 需要修复的问题

#### 中等：Multer 错误返回 HTML 而非 JSON（`routes.ts:47,178-203`）

当 multer 的 `fileFilter` 拒绝文件（不支持的格式）或文件超过 50MB 限制时，抛出的错误会传播到 Express 的默认错误处理器（返回 HTML 500 页面），而非进入路由的 `catch` 块。前端会收到一个非 JSON 的 500 响应，而不是适当的 400 响应，内容为 `{ error: 'INVALID_FILE', message: '仅支持上传 PDF 或图片附件' }`。

**原因：** 路由处理器签名为 `(req, res)`，不是 `(err, req, res, next)`。Multer 错误会绕过路由的 try/catch。

**建议的修复方案：** 将上传路由包装在一个 Express 错误处理中间件中，或者让 multer 的错误冒泡到显式处理 JSON 响应的单独错误路由器中。相关问题行：`routes.ts:47`（multer `fileFilter`）。

#### 轻微问题 1：速率限制在身份验证之前生效（`routes.ts:206`）

```typescript
router.get('/attachments/:id/download', fileDownloadRateLimit, authenticate, async ...
```

未经验证的请求可以在被拒绝之前消耗 120 次/15 分钟速率限制配额。交换顺序为 `authenticate, fileDownloadRateLimit` 会更合适。与 120/15 分钟的限额相比，实用性风险很低。

#### 轻微问题 2：上传文件名中潜在的多字节字符问题（`routes.ts:30, service.ts:221`）

`file.originalname`（由 multer 提供）在有特殊 UTF-8 字符的文件名上，可能包含在操作系统之间不一致的编码。文件名未经净化为 ASCII 或简单字节模式。在实践中，所有常见的浏览器/操作系统组合都能正确处理这个问题，但如果你严格遵循"安全文件名"实践，可以考虑进行清理。

---

### 评分卡

| 评分项 | 状态 |
|--------|--------|
| 无阻塞性问题 | ✅ |
| 租户隔离 | ✅ |
| 无劳务模块耦合 | ✅ |
| 无路径遍历漏洞 | ✅ |
| 文件类型限制已强制执行 | ✅（multer fileFilter） |
| 下载速率限制 | ✅（但顺序有误） |
| 烟雾测试覆盖了全 CRUD | ✅ |
| 伪造完成迹象 | 无 |
| Multer 错误 → 正确的 JSON | ❌（返回 HTML） |
