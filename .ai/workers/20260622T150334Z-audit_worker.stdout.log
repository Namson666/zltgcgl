## Audit Report: 供应商 & 班组 CRUD

**审核范围**: backend routes/service, frontend pages/api, Playwright smoke test

---

### BLOCKING（1 项）

**1. WorkTeam 按班组长搜索会崩溃**
- 文件: `backend/src/modules/wms/service.ts:1463`
- 代码: `where.OR = [{ name: { contains: search } }, { leader: { contains: search } }]`
- 问题: Prisma `WorkTeam` 模型的字段是 `leaderName`，不是 `leader`。`leader` 是 `.map()` 阶段（line 1470）才添加的计算别名。Prisma 在运行时收到 `leader` 参数会抛出 `Unknown arg` 错误。
- 后果: 用户在班组管理页搜索框输入班组长姓名 → 后端 500 错误。
- 修复: 把 `leader` 改为 `leaderName`。

---

### YELLOW（2 项）

**2. WorkTeam 删除无引用检查**
- 文件: `backend/src/modules/wms/routes.ts:1211`
- WorkTeam 已被 `OutboundOrder`、`SubContract` 等引用时，硬删除会导致外键约束异常或孤立记录。
- 建议: 至少做 `_count` 检查（类似 supplier delete），有引用时拒绝删除。

**3. Supplier 删除是硬删除**
- `routes.ts:1149` 使用 `prisma.supplier.delete()` 真删。
- 对参考数据（供应商档案）来说可能 OK，但如果用户将来有恢复需求，建议改成软删除。

---

### PASS（5 项）— 全部正确

**字段映射**
- `Supplier.contact / contactName`: 后端 create/update 同时接受 `contactName ?? contact`，list 响应映射 `contactName → contact`，前端显示/编辑兼容两个字段。✅
- `WorkTeam.leader / leaderName`: 后端 create/update 同时接受 `leaderName ?? leader`，list 响应映射 `leaderName → leader`，前端显示/编辑兼容两个字段。✅

**租户隔离**
- 供应商: 增查改删全部 `findFirst({id, tenantId})` + create 写 `tenantId` ✅
- 班组: 同上 ✅
- 不会越租户操作。✅

**Playwright 测试**
- 供应商: 真实创建 → 编辑 → 删除，填写完整表单（name/contact/phone/address/bank），验证 toast 成功消息和表格行消失。✅
- 班组: 真实创建 → 编辑（含人数变更） → 删除，验证 toast 和表格行变化。✅
- 不是只看页面能打开，是完整的 CRUD 交互覆盖。✅

**删除不误删其他租户**
- Update/delete 之前都先 `findFirst({ id, tenantId })` 验证归属。✅

---

### 建议但不阻塞

- `service.ts:1436` `listSuppliers` 的搜索参数名叫 `name` 但接收的是 keyword/search，语义混淆但逻辑正确。
- 修复项 #1 后建议运行一次 `tsc --noEmit` 验证类型。
