审查完毕。以下是完整报告。

---

## AUDIT: 生产级人脸识别 Provider 接入点

**PASS**（有条件通过）

---

### BLOCKERS: none

检查流的每一条救生路径都是通的：

1. **打卡永不阻断** — `createMobileCheckIn` (service.ts:1317) 在 `faceResult` 返回后无条件执行 `prisma.mobileCheckInRecord.create()`；`faceStatus` 仅作为元数据存储，不影响记录写入。

2. **所有降级路径都返回「保留打卡数据」** — provider 配置缺失返回 `not_configured`，网络/超时返回 `provider_error`，照片缺失返回 `provider_error`，每种都声明"已保留打卡数据等待后续核验"。没有 `throw`。

3. **凭证从环境变量读取** — `FACE_RECOGNITION_HTTP_API_KEY`，未硬编码，发送为 `Authorization: Bearer`。`fetch` 走 HTTPS。

4. **多租户隔离正确** — `faceProvider` 存储在每个租户的 `attendanceSetting` 表中，通过 `tenantId` 隔离。写操作受 `requirePermission('canManageAttendance')` 保护。服务端对 provider 值做白名单校验。

5. **超时安全** — `AbortController` + `FACE_RECOGNITION_TIMEOUT_MS`（默认 8s），超时降级为 `provider_error`。

---

### YELLOW RISKS

#### Y1. `resolveUploadPath` 缺乏路径遍历防护

**位置**: `face-provider.ts:30-35`

```typescript
function resolveUploadPath(photoUrl: string) {
  const uploadRoot = path.resolve(process.env.UPLOAD_DIR || ...);
  if (photoUrl.startsWith('/uploads/')) {
    return path.join(uploadRoot, photoUrl.replace('/uploads/', ''));
  }
  // ...
}
```

`photoUrl` 为 `/uploads/../../etc/passwd` 时，`path.join(uploadRoot, '../../etc/passwd')` 会逃逸出 `uploadRoot`。

**缓解**: 当前实践中，`photoUrl` 由 multer 在合法上传时设置（routes.ts:160 → `/uploads/<uuid>.jpg`），并存储在数据库中。因此在实际运行中路径是安全的。

**建议**: 加一个校验：

```typescript
const resolved = path.resolve(uploadRoot, relative);
if (!resolved.startsWith(path.resolve(uploadRoot))) {
  return null; // path traversal attempt
}
```

#### Y2. `parseFaceResponse` 的响应解析过于宽容

**位置**: `face-provider.ts:72-82`

对于所有未知 provider 的响应，代码会遍历 `body?.data ?? body?.result ?? body`、`data?.score ?? data?.similarity ?? body?.score ?? body?.similarity` 等。一个格式错误的非 JSON 响应（例如 HTML 404 页面）会导致第 76 行的 `.json()` 返回 `{}`，然后 `parseFaceResponse` 将所有 `undefined` 推断为 `score < threshold`，产生 `mismatch` 状态。

这不是安全漏洞，但**诊断困难**：运维人员看到 `mismatch`，会以为是照片不匹配，而非配置/网络问题。

**建议**: 如果服务器返回非 2xx 状态码，已由 `!response.ok` 分支提前降级为 `provider_error` 并包含状态码。可考虑在 `catch` 中也记录原始响应文本用于调试。

#### Y3. 前端显示未校验的 provider 值，选择框可能不同步

前端下拉框的选项为 `stub/http/tencent/baidu/aliyun`，但后端 `HTTP_PROVIDER_ALIASES` 额外包含 `cloud`，而 `allowedProviders`（service.ts）又排除了 `cloud`。如果 `cloud` 通过直接 DB 操作写入，前端选择框将显示为空（无匹配选项），但 `saveMobileRule` 会校验失败。这是不一致但非关键问题。

---

### 测试覆盖评估

| 测试范围 | 结果 |
|---------|------|
| Provider 配置缺失 | ✅ `returns not_configured` |
| HTTP 网关正常匹配 | ✅ `accepts a match` |
| 低分返回 mismatch | ✅ `marks low score responses as mismatch` |
| 网络失败降级 | ✅ `degrades to provider_error` |
| 照片缺失降级 | ✅ `degrades to provider_error when missing` |
| 远程 URL 照片 | ✅ `sends remote photo URLs` |
| Data URI 照片 | ✅ `accepts data URIs` |
| Smoke: 切换 provider 并保存 | ✅ `browser-smoke.spec.ts` 已覆盖 |
| **端到端：createMobileCheckIn + face mismatch→checkin仍保存** | ❌ **缺失** |
| **端到端：createMobileCheckIn + provider_error→checkin仍保存** | ❌ **缺失** |
| **阈值边界测试（score == threshold）** | ❌ **缺失** |

---

### 建议的最小修复项

1. **加一个 `resolveUploadPath` 路径遍历校验**（Y1 修复，5 行代码）
2. **(可选) 补充一个 `createMobileCheckIn` 集成测试**，验证人脸验证失败时打卡记录依然创建 `faceStatus: 'mismatch'`
