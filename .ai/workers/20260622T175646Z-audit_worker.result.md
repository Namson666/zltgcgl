# Claude Worker Result: 20260622T175646Z-audit_worker

- role: `audit_worker`
- status: `PASS`
- exit_code: `0`
- prompt: `.ai/workers/20260622T175646Z-audit_worker.prompt.md`
- stdout: `.ai/workers/20260622T175646Z-audit_worker.stdout.log`
- stderr: `.ai/workers/20260622T175646Z-audit_worker.stderr.log`
- diff: `.ai/workers/20260622T175646Z-audit_worker.diff.patch`

## Worker stdout excerpt

```text
nvoices/:id` | `findUnique({id})` 跨租户可读 | `findFirst({id, tenantId})` ✅ |
| `PUT /finance/invoices/:id` | 无租户校验 | 先 `findFirst({id, tenantId})` 校验再 update ✅ |
| `DELETE /finance/invoices/:id` | `delete({id})` 任意删 | 先校验再删 ✅ |
| `POST /finance/invoices` | 无 contract 租户校验 | `assertFinanceContractBelongsToTenant` ✅ |
| `GET /finance/receipts/:id` (新增) | 不存在 | `findFirst({id, tenantId})` ✅ |
| `PUT /finance/receipts/:id` | 无校验 | 同发票模式 ✅ |
| `DELETE /finance/receipts/:id` | 无校验 | 同发票模式 ✅ |
| `POST /finance/receipts` | 无校验 | 合同+发票双租户校验 ✅ |
| `getContractPnl` | `where: {contractId}`（无 tenantId） | `where: {tenantId, contractId}` 三处聚合 ✅ |

**这是真实的安全修复**：原 `getContractPnl` 用 `contractId` 直接聚合 invoice/receipt/expense，跨租户合同 ID 碰撞就会泄露金额。已加 `tenantId` 过滤。

---

## 3. 收款↔发票↔合同跨实体一致性 ✅

新增两个 helper（service.ts）：

```ts
assertFinanceContractBelongsToTenant(tenantId, contractId)
assertInvoiceBelongsToTenant(tenantId, invoiceId, contractId)
  // 校验发票存在且 invoice.contractId === 传入的 contractId
```

- `createReceipt` / `updateReceipt` 都在写入前调用两者。
- 跨租户发票 ID 直接返回 `INVOICE_NOT_FOUND`，跨合同错挂发票返回 `INVOICE_CONTRACT_MISMATCH`。
- `updateReceipt` 校验使用 `nextContractId = data.contractId ?? existing.contractId`，避免 contractId 不传时跳过校验。

---

## 4. 导入导出真实文件流 ✅

**Import**：路由现在接 `multer.memoryStorage()` + `importUpload.single('file')`，处理两种入口：
1. `req.body.rows`（旧 JSON 路径，兼容）
2. `req.file.buffer` → `JSON.parse(...)`（新 multipart 文件路径）

前端 `ImportExcel.tsx` 的 `jsonToFormData` 把数据塞进 `FormData.append('file', blob, 'import.json')`，刚好命中新分支。**真实多部分上传不是假数据**。

**Export**：
- 新增 `exportRowsToExcel` 用 `exceljs` 真实生成 `.xlsx` Buffer。
- `/finance/export` 和 `/finance/export-summary` 通过 `sendExcel(res, buffer, filename)` 发送 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` + RFC 5987 文件名。
- 前端 `handleExportLedger/handleExportSummary` 接收 Blob 走 `downloadBlob` 触发下载。
- `financeApi.exportExcel/exportSummary` 已带 `{ responseType: 'blob' }`。

**真实下载链完整**。

---

## 5. Chrome smoke 覆盖 ✅

新增 `enterprise user can complete finance invoice receipt pnl import export CRUD` (browser-smoke.spec.ts:680-836)，覆盖：

| 步骤 | 验证 |
|---|---|
| 新增发票 | toast + 列表可见 |
| API 查询 `invoiceTotal >= 888.88` | ✅ |
| 新增收款（选合同 → 等发票 option 出现 → 选 → 填金额/付款方/账户/流水号/备注） | ✅ |
| API 校验 `createdReceipt.invoiceNumber === invoiceNo` | ✅ |
| 编辑收款（改付款方）→ 列表过滤可见 | ✅ |
| 详情面板展示关联收款明细 | ✅ |
| 编辑发票 | ✅ |
| PnL API 校验 `invoiceTotal >= 888.88 && receiptTotal >= 388.88` | ✅ |
| 合同盈利分析页面可见该合同 | ✅ |
| 真实 CSV 写入临时文件 → `setInputFiles` → 解析 → 确认导入 → 跳"导入结果"对话框 | ✅ |
| 导出费用台账 + 财务汇总（两次 `waitForEvent('download')` 验文件名 + toast） | ✅ |
| 删除收款 / 删除发票 → 列表不再显示 | ✅ |

**真实 Chrome / Playwright / 真实磁盘文件 / 真实下载事件**——不是 Product Green。

---

## 6. 权限 / 假数据 / 死按钮审计 ✅

- 新增 `getReceiptDetail` 路由没有单独挂 `requirePermission`，但路由前缀 `receiptRouter` 已经在 `router.use('/receipts', authenticate, requireUser, ...)`，GET 类端点无需权限（合理：查看）。✅
- `assertFinanceContractBelongsToTenant` 仅校验存在性，没校验 `isActive`——非阻塞建议：若合同被软删仍可挂账。
- `getReceiptsSummary` / `getOverdueReceipts` 在原 service 中**仍用 `where: { contractId }` 聚合内部 invoice**（service.ts:1228-1239、1262），按 contractId 遍历，没 tenantId 隔离风险（外层已 `findMany({tenantId})` 限制 contracts 列表），但循环内 `prisma.finInvoice.findMany({where: {contractId: contract.id}})` 仍是按 contractId 查询。**理论安全**（不会跨租户，因为 contracts 已限定），但若合同 ID 全局唯一但删除时未级联，存在僵尸数据泄漏。**非阻塞**。

---

## 阻塞问题

**无**。

## 非阻塞建议

1. **`getContractPnl` 仍残留风险**：`finExpense.findMany` 虽已加 `tenantId`，但 `subContract` / `progressPayment` / `subProgressPayment` / `outputValue` 仍是 `where: { contractId }`，外层 contract 已限定所以实际安全，但未来若有人按非合同维度查询会泄漏。建议统一加 `tenantId`。
2. **`importUpload` 没加 `fileFilter`**：5MB 限制 OK，但任何 MIME 都收。当前前端固定发 `application/json`，OK；若开放给外部用户应加白名单。
3. **`/finance/import` 错误码重复**：JSON 解析失败抛 `INVALID_IMPORT_FILE`，但前端 catch 后 toast 文案可能误导（前端发的是 multipart blob，OK；外部脚本可能踩）。
4. **`assertFinanceContractBelongsToTenant` 只查存在性**，不查 `isActive`/`deletedAt`——可挂账到软删合同。非阻塞。

---

**审计结论：本次改动把财务发票/收款的 tenantId 隔离、字段对齐、真实导入导出、真实浏览器 smoke 都做齐了，并修复了 `getContractPnl` 跨租户聚合的真实安全漏洞。PASS。**

```

## Worker stderr excerpt

```text

```
