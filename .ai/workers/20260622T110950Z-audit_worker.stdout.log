我现在已完成对工作树的完整审查。以下是审计报告。

---

## Phase 1 租户模块权限审计报告

### 1. 阻塞性问题

**无。** 该实现架构健全：路由守卫链（`authenticate → requireUser → requireTenantModule`）为 WMS/Labor/Finance 路由提供了正确的租户隔离模块级访问控制。开发者端点有 `requireDeveloper` 保护。前端使用 `hasModule()` 进行路由门控和菜单可见性。Prisma 模型具有正确的双重唯一约束（`@@unique([tenantId, moduleKey])`）和外键级联删除。

### 2. 需要关注的次要问题

**a) 自注册流程缺少即时模块权限（前端空白期）**
`auth/routes.ts` 中的 `POST /api/auth/register` 处理器（第 898-1079 行）在事务中未创建 `TenantModuleEntitlement` 记录（不同于开发者管理的 `createTenant`，后者在第 258-266 行正确地创建了记录）。此外，其响应负载也未包含 `enabledModules`。

该问题会在运行时由 `getTenantEnabledModules` 中的 `ensureTenantModuleEntitlements` 隐式修复（该函数通过 upsert 循环惰性创建缺失的记录，第 19-58 行），且 `/auth/me` 端点也会调用它（第 672 行）。但实际效果是：刚自注册的用户在**页面刷新之前**会在前端看不到任何模块门控菜单——`hasModule()` 对所有模块键均返回 `false`。

**建议**：在 `register` 事务中创建三个 `TenantModuleEntitlement` 记录（与开发者 `createTenant` 的做法一致），并将 `enabledModules` 包含在注册响应中，以消除前端空白期。

**b) `updateTenantModuleEntitlements` 中的 `enabledAt` 会重置现有记录**
每次进行 upsert 且 `isEnabled: true` 时（第 82 行），`enabledAt` 均会被设为 `new Date()`，即使该行之前已启用。切换 `true → false → true` 后，时间戳会丢失。这属于语义取舍，并非 bug——请确认业务团队是否预期 `enabledAt` 仅记录首次启用。

**c) `setTenantModules` 中对无效模块返回 500 而非 400**
`service.ts` 第 304-305 行在模块键无效时抛出，路由处理器的 `catch` 块（第 221-226 行）会将其作为 500 `INTERNAL_ERROR` 返回。更好的做法是使用明确的 400 错误。在 Phase 1 现有实现中，用户体验表现为「保存失败」，而非清晰的「不支持的模块：xxx」。

### 3. 安全与租户隔离风险

| 领域 | 评估 |
|----------|--------|
| 路由守卫顺序 | `app.ts` 中 `authenticate → requireUser → requireTenantModule('wms')` 的顺序正确。所有 6 个模块门控路由均被同等保护。 |
| dev-view 处理 | `requireUser` 中间件（第 124-165 行）正确地允许 dev-view 开发者通过（显式 `isDevView` 标记 + 基于 `type/tenantId` 的续期回退），同时确保 `tenantId` 存在。 |
| JWT 信任 | 守卫信任 `req.user.tenantId`，该值来自经正确验证的 JWT。只要 JWT 签名密钥保密，此方案安全。 |
| 开发者端点保护 | `GET/PUT /tenants/:id/modules` 受 `requireDeveloper` 保护（第 15 行使用 `router.use`）。正确。 |
| 前端门控 | `hasModule()`（第 443-448 行）对非 dev-view 的开发者返回 `true`，对其他所有人检查 `enabledModules`。路由层在 `ModuleRoute` 中执行重定向，菜单通过 `isMenuItemVisible`/`isMenuGroupVisible` 实现隐藏。正确——双重防护。 |
| 合同管理隔离 | 按要求实现：`/api/contracts` 路由（第 92-93 行）**未**使用 `requireTenantModule`，前端路由 `/contracts` 也未使用 `ModuleRoute`。正确。 |
| 现有租户默认值 | `ensureTenantModuleEntitlements` 中的惰性创建 upsert 所有三个模块，且 `isEnabled: true`。对于尚未创建记录的现有租户，首次访问时即可开通。正确。 |

**未发现租户隔离漏洞。** 该架构防止了跨租户的模块访问。

### 4. 测试缺口（Phase 1 标记为 Green 前必须关闭）

当前项目中**没有任何测试**覆盖模块权限系统：

- `backend/src/__tests__/`：仅有 2 个测试文件（`auth.test.ts`，`health.test.ts`）
- 对 `requireTenantModule` 中间件：**0 个测试**
- 对 `module-entitlement.service.ts`：**0 个测试**
- 对 `isTenantModuleEnabled` / `ensureTenantModuleEntitlements`：**0 个测试**
- 对 `hasModule()` 前端行为：**0 个测试**
- 对 `ModuleRoute` 前端组件：**0 个测试**

**所需的最低测试：**

| # | 测试 | 原因 |
|---|------|------|
| T1 | `requireTenantModule`：模块启用时允许通过 | 守卫路径 |
| T2 | `requireTenantModule`：模块未启用时返回 403 | 守卫路径 |
| T3 | `requireTenantModule`：无 `tenantId` 时返回 401 | 守卫路径 |
| T4 | `ensureTenantModuleEntitlements`：为现有租户创建缺失的模块 | 数据库迁移路径 |
| T5 | `updateTenantModuleEntitlements`：切换 `isEnabled` | 开发者 UI 路径 |
| T6 | `getTenantEnabledModules`：过滤已禁用的模块 | 业务逻辑正确性 |
| T7 | 自注册事务后模块权限存在 | 注册流程 |

### 5. 详细审查：无需关注的更改

**OCR 辅助函数修复**——重构了 `baidu-ocr.ts` 和 `ocr-ai.ts`，新增了类型守卫（`isBaiduOcrResponse`）和提取函数（`extractAiContent`，`normalizeOptionalString`）。这些更改是纯重构，功能相同，且与模块权限正交。已读取——无发现问题。

### 6. 结论

**PASS_WITH_NOTES**

未发现阻塞性问题，也未发现安全漏洞。主要关注点是：

1. **自注册流程应创建模块权限记录**并包含 `enabledModules` 响应，以避免用户首次注册时的前端空白期（约 30 分钟修复）。
2. **在启用前应添加最低限度的测试覆盖**（T1-T3，以及 T7）。当前为 0%。
3. `updateTenantModuleEntitlements` 中 `enabledAt` 的重置行为属于语义取舍，应由业务方确认。
