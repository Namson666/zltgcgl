你是独立终审 worker。请审查本轮最终改动是否满足用户要求：“删除库存预警，这个功能不需要了。”

重点改动：
- 删除 `backend/prisma/schema.prisma` 中 `Material.alertThreshold`。
- 删除 `backend/prisma/migrations/20260424_init/migration.sql` 中 `materials.alertThreshold` 初始化列。
- `scripts/verify.sh` 新增先运行 `npm run prisma:generate`，避免 schema 改动后使用旧 Prisma Client。
- `scripts/browser-smoke.sh` 新增先运行 `npm run prisma:generate`。真实 Chrome 首次暴露 WMS 入库 500（旧 client 仍查询 alertThreshold），加 generation 后重跑 12/12 通过。
- 状态/README/交接文件同步记录。

请检查：
1. 是否还存在库存预警产品功能或字段残留。
2. 是否误删劳资风控预警、人脸识别阈值、系统公告 warning 等非库存预警功能。
3. 在 verify/browser smoke 入口强制 Prisma generate 是否合理，是否有明显副作用。
4. 本轮证据是否足够：`bash scripts/verify.sh` 通过；`bash scripts/browser-smoke.sh` 真实 Chrome 12/12 通过；code-review-graph risk 0.50。

输出：
- PASS 或 BLOCKED
- blockers（如无 none）
- yellow risks（如有）
- 简短理由
