# Claude Worker Result: 20260623T051829Z-audit_worker

- role: `audit_worker`
- status: `PASS`
- exit_code: `0`
- prompt: `.ai/workers/20260623T051829Z-audit_worker.prompt.md`
- stdout: `.ai/workers/20260623T051829Z-audit_worker.stdout.log`
- stderr: `.ai/workers/20260623T051829Z-audit_worker.stderr.log`
- diff: `.ai/workers/20260623T051829Z-audit_worker.diff.patch`

## Worker stdout excerpt

```text
isApiKeyActive`。
- 解包 `body.data || body` 同样修复了 rawKey 弹窗取不到值（`createApiKey` 返回 `{success, data: {id, rawKey, ...}}`）。

启停功能真实修复。✓

---

## 3. 真实 Chrome 测试覆盖（`browser-smoke.spec.ts:730-884`）

| 要求 | 测试断言 | 状态 |
|---|---|---|
| 录入（新增系统配置） | UI 填表 → `toast 配置已添加` → 行 `toBeVisible` → API 回读 `value/description` 一致 | ✓ |
| 行内编辑 | UI 改 value → `toast 配置已保存` → API 验证 value 改变 | ✓ |
| 删除 | 行删除 → 确认弹窗文案校验 → `toast 配置已删除` → 行消失 → API 记录 undefined | ✓ |
| 权限 403（GET） | 企业用户 GET `/api/developer/system-config` → 403 | ✓ |
| 权限 403（PUT） | 企业用户 PUT `/api/developer/system-config` → 403 | ✓ |
| 录入（生成 API Key） | UI 表单 + rawKey 弹窗出现 + `rawKey.match(/^zlt_/)` + `keyPrefix === rawKey.slice(0,8)` | ✓ |
| 列表回读 | API 返回 `tenantId/keyPrefix/isActive` 三字段 | ✓ |
| 状态变化（停用） | UI 点击 → `toast 密钥已停用` → API `isActive === false` → 行文本 `停用` | ✓ |
| 状态变化（启用） | UI 点击 → `toast 密钥已启用` → API `isActive === true` | ✓ |
| 权限 403（GET/POST/PUT/DELETE） | 4 个 403 全断言 | ✓ |
| 删除 | UI 删除 → 确认文案校验 → 行消失 → API 记录 undefined | ✓ |
| 截图 | `docs/smoke-evidence/开发者系统配置API密钥CRUD.png` (84915 字节，已生成) | ✓ |

测试断言覆盖了"UI 操作 + 持久化回读 + 状态变化 + 权限"四维度，不是 Build Green。✓

---

## 4. 安全问题

- **rawKey 泄露面**：
  - `createApiKey` 后端一次性返回 `rawKey`（`service.ts:635-638`），`prisma` 只存 `keyHash`（sha256）。
  - 前端**只**在 `showKeyModal` 弹窗里设置 `rawKey`，弹窗关闭时 modal 不卸载 rawKey state（仅 `setShowKeyModal(false)`），但 React 状态在 modal 不可见时仍留在内存——属于"内存暂留"，非"网络/列表泄露"。
  - 列表渲染用 `key.keyPrefix?.slice(0,7) + '...'`（line 401），**完整密钥不进列表 DOM**。✓
  - 测试用 `rawKey.match(/^zlt_/)` 与 `keyPrefix === rawKey.slice(0,8)` 双向校验，证明后端确实没把完整 key 推回列表。
  - `visibleKeys` 切换只显示 `keyPrefix` 全长（≤8 字符），不是 rawKey。

- **权限 403 路径**：`router.use(authenticate, requireDeveloper)`（`routes.ts:15`）覆盖整个 `/api/developer/*`，企业用户 token 必然 403。4 个 403 测试真实命中。✓

- **密钥存储**：后端只存 sha256 hash（`service.ts:636`），不可逆。✓

- **复制到剪贴板**：`navigator.clipboard.writeText(rawKey)` + `readOnly textarea`，无 XSS 面。✓

无密钥泄露到列表或网络。✓

---

## 5. 假绿/死按钮/权限漏测检查

| 风险 | 检查结果 |
|---|---|
| 旧 `{status: ...}` 死按钮 | 已修：传 `{isActive}` 真实写入；测试断言 `isActive === false/true` 切换 |
| 旧 `res.data \|\| res` 列表空 | 已修：双重解包 `body.data \|\| body`；测试断言 `configs.length` 找到新建项 |
| 旧 rawKey 弹窗不显示 | 已修：双层解包；测试断言 `密钥已生成` 弹窗 + rawKey 长度 > 0 |
| 假绿：仅 UI 断言无 API 回读 | 已加：每个变更后都 GET 后端再断言 |
| 权限漏测 | GET/PUT（系统配置） + GET/POST/PUT/DELETE（API Key）共 6 个 403 全测 |
| 列表残留 | 删除后断言 `apiKey === undefined` |
| 死循环/旧 toast 文案 | 测试断言 `toast 配置已添加/已保存/已删除/密钥已停用/已启用/已删除/生成成功`，与新文案对齐 |

未发现假绿或权限漏测。✓

---

## Blockers

无。

---

## Yellow Risks

### Y-1：rawKey 内存残留

`rawKey` 状态在 `showKeyModal=false` 后仍驻留 React state，下一次模态打开会重新覆盖，但中间不会主动清空。攻击场景：浏览器控制台读取内存 / React DevTools 探查。

**建议**（非阻塞）：
```ts
onClose={() => { setShowKeyModal(false); setRawKey(''); }}
```
或点击"我已保存"时清空。

### Y-2：`isApiKeyActive` 优先级对真实数据反直觉

```ts
key.status ? key.status === 'active' : key.isActive !== false;
```
- 后端 schema 无 `status` 字段，新数据 `key.status === undefined`，永远走 `isActive !== false` 分支。✓
- 但若未来加 `status` 字段且默认 `'active'`，`isActive=false` 但 `status='active'` 会显示为启用——**与后端真值冲突**。

**建议**：把判断倒置成 `key.isActive ?? key.status === 'active'`，让后端真实字段优先生效。

---

## 建议修复项（非阻塞）

1. **过期时间检查**：`isApiKeyActive` 未考虑 `expiresAt` 已过期。API Key 即使 `isActive=true` 但已过期也应在 UI 上标"已过期"，否则运维误判。`useEffect` 加 `Date.parse(expiresAt) < Date.now()` 判断。
2. **复制反馈**：`handleCopyKey` 的 `setTimeout(() => setCopied(false), 2000)` 在用户关闭弹窗后 state 已卸载，无副作用但属于 noop——可清理。
3. **删除 API Key 不可恢复**：`handleDelete` 无 `expiresAt` 软删除窗口。已删即无，符合期望，无需改。
4. **测试独立性**：新测试用 `createSmokeTenant` 创建企业、与之前 4 个 test 共享 `zlt_browser_smoke_dev_config_api_keys.db`，时间戳隔离 OK；其他 test 没有依赖这个文件，无交叉污染。
5. **StatusBadge 文案**：`type="default"` 时停用显示灰底，与表格行的"启用"绿底有视觉差异但无信息丢失。✓

---

## 总结

本轮是**真实功能闭环修复**，不是 Build Green：
- 修了 2 个**死按钮/假列表**的 Product Green 漏洞（解包 + 字段名错配）。
- 修了 1 个**安全关闭路径**（rawKey 不入列表）。
- 新增测试用真实 Chrome 覆盖 UI 录入/编辑/状态/删除/403 五个维度，**每步都用 API 回读验证持久化**。
- 后端契约（`isActive`、`keyPrefix: rawKey.substring(0,8)`、rawKey 只在 create 返回）经源码核对全部一致。
- 截图证据已生成（84915 字节，非空壳）。

**Verdict: PASS** — 可入库/合并。Yellow Risks 建议下轮收口。

```

## Worker stderr excerpt

```text

```
