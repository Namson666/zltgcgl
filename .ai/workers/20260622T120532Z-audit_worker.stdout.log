## 审核结论：**PASS_WITH_NOTES**

### 1. 临时 SQLite 数据库 — ✅ 通过

`start-browser-smoke-server.sh` 使用 `/private/tmp/zlt_browser_smoke.db`（可通过 `BROWSER_SMOKE_DB_PATH` 覆盖）。启动前 `rm -f`，用原始 `.sql` 迁移文件 + `DATABASE_URL=file:$DB_PATH` 传给 `prisma/seed.ts`，后端也通过 `DATABASE_URL=file:$DB_PATH` 使用该临时库。开发者数据库从未被引用。

### 2. 测试环境变量 — ✅ 通过

后端启动时传入：
- `DATABASE_URL` → 临时路径
- `JWT_SECRET=browser-smoke-jwt-secret-change-me` → dummy 值（满足 `jwt.ts` 启动校验）
- `PORT=4101` → 独立端口，不干扰 dev 服务
- `NODE_ENV=test`

### 3. 系统 Chrome — ✅ 通过

`playwright.config.ts` 第 29 行使用 `channel: 'chrome'`，Playwright 将启动系统安装的 Google Chrome，而非内置 Chromium。

### 4. 测试范围诚实标签 — ✅ 通过

两个测试用例：
- **企业用户**：登录 + 7 个核心页面导航 + 截图（无 CRUD、无上传/下载、无错误处理）
- **开发者**：登录 + 租户管理页面

README 明确标注"完整 CRUD/上传/下载/异常处理浏览器回归尚未建立/执行"。

### 5. 安全/可靠性问题 — ⚠️ 注意事项

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| 截图路径依赖 CWD | 低 | 它们碰巧能解析正确，但严格的解法是改用 Playwright 的 `outputDir` 作为截图路径，配合 `testInfo.outputDir` 使用，而非硬编码的 `"../docs/smoke-evidence/"` |
| 后端就绪探测缺失 | 低 | `webServer.url` 只检查前端是否提供服务；如果后端 seed 脚本耗时过长，测试可能提前介入。实际上冲突不大（Playwright 的 `timeout: 60_000` 会兜底） |
| 测试凭证硬编码 | 低 | `Admin@2024` 明文写到测试文件中。私有仓库可接受，开源前务必用环境变量替换 |
| 原始 SQL 代替 `prisma migrate deploy` | 低 | 手动执行 `.sql` 文件绕过了 Prisma 的迁移校验器，但如果 Prisma 的迁移格式发生变化，这种方式会比较脆弱 |

**总结**：该 harness 设计合理、能正常工作、范围标记诚实，适合纳入管道。无阻挡性问题。
