# Claude Worker Result: 20260623T214941Z-audit_worker

- role: `audit_worker`
- status: `PASS`
- exit_code: `0`
- prompt: `.ai/workers/20260623T214941Z-audit_worker.prompt.md`
- stdout: `.ai/workers/20260623T214941Z-audit_worker.stdout.log`
- stderr: `.ai/workers/20260623T214941Z-audit_worker.stderr.log`
- diff: `.ai/workers/20260623T214941Z-audit_worker.diff.patch`

## Worker stdout excerpt

```text
utes.ts:1256`) inherits both. `auth.ts:98-108` enforces `req.user?.type !== 'developer' → 403`. Enterprise/anonymous users cannot read readiness. Confirmed by the focused Chrome assertion at `browser-smoke.spec.ts:2187-2190` (enterprise → 403).

2. **Secret / endpoint leakage** — **CLEAN.** `service.ts` returns only the `faceDiagnostic` boolean fields (`endpointConfigured`, `apiKeyConfigured`) — never the endpoint URL or the API key value. The string `FACE_RECOGNITION_HTTP_API_KEY` appears only in the `face-provider.ts` env read, and that is the env-var *name*, not its value. `apiKey`/`apiKey.findUnique` in service.ts are unrelated — they refer to the `apiKey` model (tenant API keys), not face-gateway credentials. `frontend/src/api/index.ts:499-500` calls `/developer/readiness` with no inline secrets. Smoke test `browser-smoke.spec.ts:2186` explicitly asserts no `FACE_RECOGNITION_HTTP_API_KEY` substring appears in the response body.

3. **False Product-Green claim** — **CLEAN.** `service.ts:1019-1034` returns `status: 'warning'` whenever `faceDiagnostic.ready` is false (i.e., when `FACE_RECOGNITION_HTTP_ENDPOINT` is unset), and surfaces a clear message ("FACE_RECOGNITION_HTTP_ENDPOINT 未配置；生产人脸识别仍不可用"). Independent portal domain check (`independent_portal_domains`, `service.ts:996-1001`) explicitly states "生产还需 DNS、反代和证书验证" — does not claim DNS/cert is done. `overallStatus` only becomes `'ready'` when *all* checks pass (`service.ts:1038`). No claim that real third-party face credentials are complete.

4. **Database migration missing-table handling** — **CLEAN, not hidden.** `service.ts:932-943` `readinessQuery` intercepts Prisma `P2021` (table missing), records the table name in `missingTables`, and returns the safe fallback. The `database_schema` check (`service.ts:973-980`) then surfaces `status: 'warning'` with `message: '部分生产功能表缺失，请先执行数据库迁移'` and `detail.missingTables` is exposed in the response and rendered in the UI (`ProductionReadiness.tsx:149-153`). The error is *surfaced*, not swallowed.

5. **Accidental inventory-alert/prewarning restoration** — **CLEAN.** No occurrence of `InventoryAlert`, `inventoryAlert`, `prewarning`, `库存预警`, or `预警` in any of the four scoped files (`developer/service.ts`, `developer/routes.ts`, `ProductionReadiness.tsx`, `api/index.ts`). The seven readiness checks touch only `tenant`, `tenantModuleEntitlement`, `tenantPortalConfig`, `miniProgramConfig`, `miniProgramPhoneBinding`, `attendanceSetting` — no inventory tables.

### Non-blocking notes

- `service.ts:933-940`: The P2021 catch does not log the missing table name to the server console (only records in the response). For an internal dev-only self-check, surfacing to the operator via response is sufficient; no server-side log emission is necessary. No action required.
- `service.ts:1024-1032`: The `face_gateway` check's `detail.faceProviderCounts` reveals per-provider tenant counts but no tenant IDs or PII. Acceptable.
- `service.ts:1038`: `overallStatus: 'needs_attention'` (not `'warning'`) — a different string than per-check `warning`. The frontend `ProductionReadiness.tsx:80-81` handles both correctly. Consistent.
- `ProductionReadiness.tsx:42`: `console.error` on fetch failure is debug noise; consider replacing with a logger per `typescript/coding-style.md`. Pre-existing convention, not introduced by this change.
- `service.ts:947`: `faceDiagnostic` is computed once before `Promise.all` and not re-fetched. Acceptable — it depends only on env vars that don't change per request.

### Evidence cross-check

- `cd backend && npm test -- --run src/__tests__/auth.test.ts` 13/13 → `requireDeveloper` is intact.
- Focused Chrome: developer 200, enterprise 403 — all `readiness*` assertions, no secret substring.
- `bash scripts/verify.sh` — backend + frontend green.
- `bash scripts/browser-smoke.sh` 36/36 with `/dev/readiness` matrix.
- `code-review-graph detect-changes` gaps are covered by the three real-Chrome layers above.

**Decision: SHIP.**

```

## Worker stderr excerpt

```text

```
