请作为独立审查 worker 审查当前工作区的 Phase 4 hardening 切片。

本切片在已实现的小程序/人脸/移动打卡基础上继续补：

1. 后端信任打卡地删除接口：
   - `deleteTrustedCheckInLocation`
   - `DELETE /api/labor/attendance/mobile/trusted-locations/:id`
   - 必须租户隔离，不能删除别的企业数据。
2. 前端考勤页：
   - 移动打卡记录显示“查看照片”入口；
   - 显示个人信任打卡地列表；
   - 可以删除个人信任打卡地。
3. 人员详情：
   - 已录入人脸照片显示图片预览和打开链接。
4. 开发者后台真实浏览器验收：
   - 默认小程序配置保存；
   - 企业自有小程序配置保存。
5. 真实 Chrome smoke：
   - 小程序打卡照片入口可见；
   - 添加个人信任地后列表可见；
   - 删除信任地成功；
   - 开发者默认小程序和企业小程序配置可保存。

请检查：

- 是否有租户隔离或权限漏洞；
- 是否有未授权移动端入口扩大；
- 是否有破坏原有合同/人员/考勤功能；
- smoke 是否真覆盖上述路径；
- 是否还有阻塞问题。

只审查，不修改代码。输出阻塞问题、非阻塞建议、是否可继续提交。
