我现在有足够的信息来完成审计。以下是报告。

---

## 分包合同工作流切片审计报告

### BLOCKERS: none

在 7 个关注领域中未发现阻塞问题。以下是详细分析：

---

### 1. 租户隔离 — ✅ 通过

所有后端服务函数都基于 `req.user!.tenantId` 过滤。每条路由都提取 `tenantId` 并向下传递。具体来说：
- `listSubContracts`、`createSubContract`、`updateSubContract`、`deleteSubContract` — 在查询前都使用 `findFirst({ where: { id, tenantId } })` 进行存在性检查
- `resolveWorkTeamSubcontractor` — 通过 `tenantId` 过滤找到 `WorkTeam`，如果不存在则拒绝
- `assertConstructionContract` — 通过 `tenantId` + `type: 'CONSTRUCTION'` 过滤
- `listSubContractPayments` / `createSubContractPayment` — 通过 `tenantId` 验证分包合同，付款在 `subContractId + tenantId` 上过滤
- 附件操作 — `getManagedAttachment`/`deleteManagedAttachment` 按 `tenantId` 过滤，`entityType` 限制为 `contract` 或 `sub_contract`，以防止类型混淆

**判决：** 无法跨租户读取/关联/写入。✅

### 2. 合同基础板块不依赖 wms/labor 开通 — ✅ 通过

该代码直接查询 `WorkTeam` 和 `Subcontractor` 模型（它们存在于数据库级别），而不检查模块授权。如果租户数据库中有班组，即使 wms/labor 模块未标记为已开通，它们也会显示在下拉菜单中。这对于在统一平台上运行演示/测试的默认租户可能是正确的。这不是一个阻断问题。

### 3. 无审批流程 — ✅ 通过

代码中没有审批工作流、`status` 字段（超出 `isActive` 软删除）或批准检查。`createLog` 审计日志用于事后记录，而不是审批门控。**这与"不需要审批"要求一致。** ✅

### 4. 附件分类 — ✅ 通过（细节清晰）

- `entityType: 'sub_contract'` 用于所有分包合同附件
- `category` 字段将付款凭证（`'payment_voucher'`）与结算凭证（`'settlement_voucher'`）分开
- 前端在两个独立的部分中渲染它们，每个部分都有不同的上传按钮：`"上传凭证"` → `handleUploadClick('payment_voucher')` 与 `"上传结算凭证"` → `handleUploadClick('settlement_voucher')`
- `getManagedAttachment`/`deleteManagedAttachment` 现在接受 `entityType: { in: ['contract', 'sub_contract'] }`，允许合同附件路由同时处理两种实体类型（不会混淆）

**附注：** 测试上传 `payment-voucher.pdf` 用于付款凭证，`settlement-voucher.pdf`（新的固定文件）用于结算凭证。这意味着 Playwright 测试将按类别验证正确的文件。✅

### 5. Playwright 测试覆盖 — ✅ 全面

分包合同工作流的测试流程（第 117-168 行）按顺序覆盖：

| 步骤 | 行号 | 存在 |
|---------|-------|--------|
| 切换到分包合同选项卡 | 117 | ✅ |
| 填写创建表单（名称、金额、选择承包合同、选择班组） | 118-128 | ✅ |
| 提交并验证行可见 | 129 | ✅ |
| 打开详情弹窗 | 131-132 | ✅ |
| 验证弹窗标题 + 关联的承包合同显示 | 133-135 | ✅ |
| 新增加付款记录 | 137-141 | ✅ |
| 上传付款凭证（`payment-voucher.pdf`）| 143-147 | ✅ |
| 下载付款凭证 | 148-150 | ✅ |
| 删除付款凭证 | 151-152 | ✅ |
| 上传结算凭证（`settlement-voucher.pdf`）| 154-158 | ✅ |
| 下载结算凭证 | 159-161 | ✅ |
| 删除结算凭证 | 162-163 | ✅ |
| 关闭详情弹窗 | 164 | ✅ |
| 删除分包合同 | 166-168 | ✅ |

### 6. Schema vs Migration — 无漂移（预期模式）

迁移 SQL（`20260622_add_subcontract_work_team_flows/migration.sql`）：
```sql
ALTER TABLE "sub_contracts" ADD COLUMN "workTeamId" TEXT;
CREATE INDEX "sub_contracts_workTeamId_idx" ON "sub_contracts"("workTeamId");
```

schema.prisma 添加：
- `SubContract.workTeamId String?`
- `SubContract.workTeam WorkTeam? @relation(fields: [workTeamId], references: [id])`
- `WorkTeam.subContracts SubContract[]`
- `@@index([workTeamId])`

迁移中故意省略了外键约束（`REFERENCES work_teams(id)`）——这与该项目的现有模式一致（例如 `20260622_add_procurement_contract_flows` 也没有添加 `parentContractId` 的 FK）。Prisma 在应用层处理引用完整性，而烟雾测试服务器通过原始 SQL 迁移文件而不是 Prisma Migrate 进行引导。

`prisma validate` 错误（`DATABASE_URL` 环境变量未定义）是预期的——在没有 `.env` 的环境中运行，而不是模式错误。

**判决：** Schema.prisma 和迁移 SQL 一致。✅

### 7. 前端运行时稳定性 — ✅ 低风险

**响应结构兼容性：** 所有新的后端端点都遵循现有的 `{ success, data, message }` 信封。前端处理包含三层回退（`body.data || body || []`），以处理响应中可能出现的嵌套变体。✅

**`useCallback` 依赖关系：** `fetchContracts` 正确地依赖于 `[currentPage, keyWord, activeTab, pageSize]`。分包合同与标准合同的切换按照 `setActiveTab` + `setCurrentPage(1)` 的模式有效工作，React 将这些批量处理并触发一次重新获取。✅

**共享 ref 处理：** 两个详情弹窗（合同与分包合同）都渲染了 `<input ref={uploadRef}>`，但这没问题，因为它们互斥（弹窗不同时打开）。`handleFileChange` 通过 `detailSubContract` 存在性判断路由到正确的 API。✅

**无白屏路径：** 条件守卫涵盖所有异步路径（加载旋转、空数据、错误 toast、不为空时的数据渲染）。表格的 `activeTab === 'SUBCONTRACT'` 条件切换没有未处理的状态组合。✅

**空状态文本问题（非阻塞）：** 当没有分包合同时，EmptyState 说明文字为："分包合同新建、付款凭证、结算凭证将在后续切片接入"——但这已经是指当前代码切片。在创建第一个分包合同后，此消息会消失，但在此期间具有误导性。

---

### 非阻塞问题

| # | 问题 | 建议 |
|---|-------|------------|
| 1 | 空状态描述已过时 | 将 `List.tsx:1049` 的 `description` 改为 `"点击"新增分包合同"按钮创建第一个分包合同"` 以匹配其他选项卡。当前文本错误地暗示这些功能尚不可用。 |
| 2 | 迁移中缺少 FK 约束（模式一致） | 如果该项目的迁移策略在需要时添加 FK，则考虑添加 `REFERENCES work_teams(id)`。目前，它与添加 `parentContractId` 的现有模式一致。 |
| 3 | 共享附件状态键 | `attachments` 状态在 `detailContract` 和 `detailSubContract` 弹窗之间混合使用。由于它们互斥，这在功能上没问题，但如果在同一个组件树中同时打开两者进行调试，则可能会使调试复杂化。 |

---

### VERIFICATION_REVIEW

**Prisma 验证：** 除了预期的 `DATABASE_URL` 缺失（环境问题，而非模式问题）外，未执行。`npx prisma validate` 需要 `DATABASE_URL`。模式本身（`schema.prisma`）格式正确，没有类型错误——在 schema 中正确添加了 `workTeamId` 和关系，新的 `SubContract.workTeam` 可选关系上有 `isActive: true` 过滤。

**Shell 烟雾测试服务器：** 通过包含新的迁移 SQL 文件正确更新了 `scripts/start-browser-smoke-server.sh`。播种应该创建某些工作团队（`WorkTeam` 模型），以供分包合同测试选择。

**Playwright 烟雾测试（`browser-smoke.spec.ts`）：** 已经审查发现，测试覆盖了分包合同创建、关联承包合同、关联工作团队、付款记录、付款凭证上传/下载/删除、结算凭证上传/下载/删除以及分包合同删除。与其他测试结果（采购合同附件、发票）的交互已通过包含的休眠和等待语句正确排序。

**注意：** `docs/` 下的现有烟雾证据截图已更新（png 文件以二进制形式更改），但 `browser-smoke.spec.ts` 中的分包合同流程是新的，需要运行才能生成新的截图证据。

**判决：** 烟雾测试覆盖全面。运行 `bash scripts/browser-smoke.sh` 以生成更新的烟雾证据截图以完成 PR。
