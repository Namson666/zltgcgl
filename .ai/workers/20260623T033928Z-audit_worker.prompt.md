# Claude Audit Request: Department Reimbursement Approval Slice

Please audit the current working tree diff for the “项目部报账审核/驳回” slice.

## Scope

- Backend finance service now defaults missing `payer` to `handler` for project department reimbursements.
- Backend finance `approveExpense` / `rejectExpense` now require `tenantId` and use tenant-scoped lookup.
- Frontend finance API now exposes `rejectExpense`.
- Frontend expense list now has an “审核驳回” button for pending expenses.
- Playwright real Chrome smoke adds:
  - project department reimbursement submit with file upload
  - API verification of pending/source/payer/receiptPath
  - approval through UI
  - API verification of approved status
  - approved expense delete returns `EXPENSE_ALREADY_APPROVED`
  - second reimbursement rejection through UI
  - rejected expense deletion through UI
  - final API verification record is gone
- Backend unit test adds safeguards for payer default and tenant-scoped approve/reject lookup.

## Verification already run

- Focused Chrome:
  `BROWSER_SMOKE_DB_PATH=/private/tmp/zlt_browser_smoke_dept_approval.db npx playwright test --config playwright.config.ts -g "department reimbursement"`
  Result: 1/1 passed.
- Backend focused test:
  `npm test -- --run src/modules/finance/service.test.ts`
  Result: 1 file / 3 tests passed.
- Unified verification:
  `bash scripts/verify.sh`
  Result: backend Prisma generate, backend tests 6 files / 36 tests, backend build, frontend tests 2 files / 36 tests, frontend build all passed.
- Full real Chrome smoke:
  `bash scripts/browser-smoke.sh`
  Result: 14/14 passed in system Chrome.

## Please check

1. Any tenant-isolation regression or remaining cross-tenant approval/rejection risk.
2. Whether the new reject UI respects existing permissions and does not expose a fake-success flow.
3. Whether `payer || handler` is safe and compatible with company finance entries.
4. Whether the Playwright coverage is strong enough for this product path.
5. Whether there are any public-repo secret leaks, destructive behavior, or hidden Product Green overclaim.

Return a clear PASS/BLOCKED verdict with blockers and Yellow risks.
