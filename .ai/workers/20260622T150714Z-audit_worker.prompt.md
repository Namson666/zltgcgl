请复审上一次 audit_worker 指出的供应商/班组 CRUD 问题是否已修复。

上次阻塞问题：
- backend/src/modules/wms/service.ts 的 WorkTeam 搜索使用了不存在字段 leader，应为 leaderName。

本次修复：
- WorkTeam 搜索改为 leaderName。
- WorkTeam 删除增加 subContract/outboundOrder/returnOrder 引用检查，有引用时拒绝删除。
- Playwright CRUD 测试新增：创建班组后用班组长姓名搜索，验证不会 500 且行可见。

请检查当前工作区 diff，重点：
1. WorkTeam 搜索字段是否正确。
2. WorkTeam 删除引用检查是否租户隔离。
3. Supplier/WorkTeam CRUD 是否仍满足新增、编辑、删除需求。
4. Playwright 是否覆盖真实点击与班组长搜索。

请输出 PASS/FAIL、阻塞问题、Yellow 风险、建议。
