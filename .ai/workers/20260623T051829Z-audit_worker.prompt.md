你是独立审查 worker。请审查本轮改动是否正确补齐“开发者系统配置 + API 密钥生命周期”的真实功能闭环。

用户总要求：
- 全程 Superpowers Pro；
- 尽量调用 Claude CLI；
- 交付前必须真实浏览器点击功能板块，录入/删除测试通过；
- 使用 code-review-graph；
- 不要把 Build Green 当 Product Green。

本轮改动摘要：
- `frontend/src/pages/developer/SystemConfig.tsx`
  - 修复系统配置列表数据解包：Axios 返回 `res.data` 是 `{ success, data }`，页面现在使用 `body.data || body`，否则保存成功后列表仍显示 0 项。
  - 新增系统配置新增表单和行内编辑 testid。
- `frontend/src/pages/developer/ApiKeys.tsx`
  - 修复 API Key 前后端字段不一致：前端原来读取 `status` 并传 `{status}`，后端实际返回/接收 `isActive`。
  - 新增 `isApiKeyActive()` 兼容旧 `status`，启停时传 `{ isActive }`。
  - 修复创建后完整密钥弹窗数据解包：使用 `body.data || body`。
  - 新增 API Key 表单、原始密钥、行 testid。
- `frontend/tests/smoke/browser-smoke.spec.ts`
  - 新增真实 Chrome 测试 `developer can manage system config and api key lifecycle`：
    - 开发者 UI 新增/编辑/删除系统配置，API 回读验证；
    - 企业用户 GET/PUT developer system-config 返回 403；
    - 创建测试企业；
    - 开发者 UI 生成 API Key，验证完整密钥只在弹窗出现且前缀匹配；
    - API 回读 tenantId/keyPrefix/isActive；
    - UI 停用/启用 API Key，API 验证 `isActive` 变化；
    - 企业用户 GET/POST/PUT/DELETE developer api-keys 返回 403；
    - UI 删除 API Key，API 验证消失；
    - 生成截图 `docs/smoke-evidence/开发者系统配置API密钥CRUD.png`。

已执行验证：
- Focused real Chrome:
  `BROWSER_SMOKE_DB_PATH=/private/tmp/zlt_browser_smoke_dev_config_api_keys.db npx playwright test --config playwright.config.ts -g "system config and api key lifecycle"`
  结果：1/1 passed。
- Unified verify:
  `bash scripts/verify.sh`
  结果：backend Prisma generate、backend tests 6 files / 36 tests、backend build、frontend tests 2 files / 36 tests、frontend build 全通过。
- Full real Chrome:
  `bash scripts/browser-smoke.sh`
  结果：17/17 passed。

请重点审查：
1. SystemConfig 数据解包是否正确，不会破坏旧返回形态。
2. API Key `status`/`isActive` 修复是否正确，是否真实修复启停功能。
3. 真实 Chrome 测试是否覆盖录入、编辑/状态变化、删除、权限 403。
4. 是否有安全问题：完整 rawKey 是否只用于创建后的弹窗；是否有密钥泄露到列表。
5. 是否存在假绿、死按钮、权限漏测或 Product Green 夸大。

输出：
- Verdict: PASS / WARN / FAIL
- Blockers
- Yellow risks
- 建议修复项
